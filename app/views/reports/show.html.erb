<%= render 'report_header' %>

<div id="report_holder">

	<%= render 'report_content' %>
	
</div>

<div class="back_footer">
	<%= link_to "Edit", edit_organization_fund_company_report_path(@organization, @fund, @company, @report) %> |
	<%= link_to "Back", organization_fund_company_reports_path(@organization, @fund, @company) %>
</div>

<script>

	function recolor_month(selected_period) {
    jQuery('div.report_month_menu div').removeClass('month_selected').addClass('month_plain')
		jQuery('.report_month_menu div#' + selected_period).addClass('month_selected')
	};

</script>